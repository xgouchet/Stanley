<resources>

    <!-- Base application theme. -->
    <style name="Theme.Stanley.DayNight" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="android:colorBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/text_title</item>
        <item name="android:textColorSecondary">@color/text_subtitle</item>

        <item name="colorOnBackground">@color/text_title</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/text_title</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="android:windowContentTransitions">true</item>
<!--        <item name="android:actionMenuTextColor">@color/accent</item>-->
                <item name="android:itemTextAppearance">@style/Theme.Stanley.DayNight.PopupMenuTextAppearance</item>
    </style>

    <style name="Theme.Stanley.DayNight.PopupMenuTextAppearance">
        <item name="android:textColor">@color/text_title</item>
    </style>

    <!-- Actual app theme-->
    <style name="AppTheme" parent="Theme.Stanley.DayNight" />

</resources>
